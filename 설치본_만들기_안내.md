# 설치본(setup.exe) 만들기 안내

배포 시 PC마다 런처(bat)·경로·파이썬 환경이 달라 생기는 문제를 없애기 위해,
**더블클릭 설치 → 시작 메뉴·바탕화면 아이콘 → 더블클릭 실행**이 되는
설치본(`setup.exe`)을 만드는 방법입니다.

> 중요: 윈도우 실행파일은 **윈도우 PC에서만** 빌드할 수 있습니다.
> (맥/리눅스에서는 윈도우 exe 생성 불가) 아래 과정은 윈도우에서 진행하세요.

---

## 준비물 (윈도우 PC에 최초 1회만 설치)

1. **Python 3.12 이상** — https://www.python.org
   - 설치 화면에서 **“Add python.exe to PATH”** 를 반드시 체크
2. **Inno Setup 6** (무료, 한글 지원) — https://jrsoftware.org/isdl.php
   - 기본값으로 설치하면 됩니다.

## 프로그램 소스 가져오기

이 프로그램 폴더(`vibe-blogpost-free`)를 윈도우 PC로 가져옵니다. 방법은 둘 중 하나:

- **GitHub에서 받기(권장)**: 맥에서 최신 코드를 push 한 뒤, 윈도우에서
  `https://github.com/samho4987-rgb/vibe-blogpost-free` → **Code ▸ Download ZIP**
  로 받아 압축 해제.
- **폴더 복사**: 맥의 `vibe-blogpost-free` 폴더를 통째로 윈도우로 복사
  (단, `.venv`, `data`, `output`, `.env` 는 빼고 복사하는 게 깔끔합니다).

## 빌드 (명령 없이 더블클릭 한 번)

압축을 푼 폴더 안 `scripts` 폴더에서 **`build_windows_installer.bat`** 를 더블클릭합니다.

스크립트가 자동으로:
1. 빌드용 가상환경 생성 → 2. 의존성 설치 → 3. PyInstaller로 앱을 폴더형으로 묶기
→ 4. Inno Setup으로 설치본 생성 까지 진행합니다.

완료되면 다음 파일이 만들어집니다:

```
dist\vibe-blogpost-free-setup-1.0.1.exe
```

이 **`setup.exe` 하나만** 배포하면 됩니다.

## 받는 사람(사용자) 입장

1. `setup.exe` 더블클릭 → 설치(관리자 권한 불필요)
2. **시작 메뉴 › “네이버 블로그 매물 자동화”** 또는 바탕화면 아이콘으로 실행
3. 크롬(Chrome)만 설치돼 있으면 됩니다.

- 설정·로그인 세션·생성물은 `내 PC의 사용자 폴더(%LOCALAPPDATA%\vibe-blogpost-free)`에
  저장되어, 재설치·업데이트에도 그대로 보존됩니다.
- 제거는 **제어판 › 프로그램 제거** 또는 시작 메뉴의 “… 제거” 로.

---

## 새 버전 낼 때

1. `naver_blog_automation/version.py` 의 `APP_VERSION` 을 올림 (예: `1.0.2`).
2. `scripts\build_windows_installer.bat` 더블클릭 → 새 `setup.exe` 생성.
   (설치본 파일명 버전은 `version.py` 값을 자동으로 따라갑니다.)
3. 배포(원하면 GitHub Releases에 첨부).

## 참고 / 문제 해결

- **“Inno Setup 이 설치되어 있지 않습니다”** 안내가 뜨면: Inno Setup 6 설치 후 다시 실행.
  (이 경우에도 `dist\vibe-blogpost-free\` 폴더형 빌드는 이미 만들어져 있어,
  그 안의 `vibe-blogpost-free.exe` 를 바로 실행해 확인할 수 있습니다.)
- **크롬이 없는 PC**: 이 프로그램은 설치된 크롬으로 네이버를 자동화합니다.
  받는 분 PC에 크롬을 먼저 설치해 주세요.
- 설치본은 브라우저(크롬)를 포함하지 않아 용량이 작습니다(수십 MB 수준).
