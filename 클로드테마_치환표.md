# vibe-blogpost-free · claude_theme 적용 치환표 (2026-07-29)

> 대상: `2_완성_운영중/vibe-blogpost-free/app.py` (3,929줄)
> 테마 모듈: 같은 폴더 `claude_theme.py` · 미리보기 `demo_theme.py`
> 이 표는 **읽기 전용 조사** 결과다. app.py 는 아직 한 글자도 고치지 않았다.

---

## 0. 한눈에

| 단계 | 무엇을 | 지점 수 | 성격 |
|---|---|---|---|
| 1단계 | `_configure_style()` 본문 → `apply_claude_theme(self.root)` 한 줄 | 95줄 삭제 | 즉시 |
| 2단계 | 리터럴 색 치환 | **90곳** | 기계적 |
| 3단계 | 심볼(`self.NAVY` 등) 치환 | **22곳** | 기계적 + 함정 2개 |
| 4단계 | 띄워보고 손보기 | — | 눈으로 |

앱 전체 색 사용 지점은 111곳인데, 그중 **21곳은 `_configure_style()` 안에 있어 1단계에서 통째로 사라진다.**
그래서 손으로 만질 곳은 111 − 21 = **90곳**(+ 심볼 22곳)이다.

---

## 1단계 — `_configure_style()` 교체

`app.py` **L405 ~ L499 (95줄)** 이 함수 본문 전체.

```python
    def _configure_style(self) -> None:
        apply_claude_theme(self.root)
```

상단에 `from claude_theme import apply_claude_theme, C` 추가.

**왜 이것만으로 대부분이 바뀌나** — app.py 가 쓰는 ttk 스타일 이름을 `claude_theme.py` 가 그대로 유지한다.
실제로 app.py 에서 쓰이는 이름과 횟수:

`White.TFrame` 39 · `Secondary.TButton` 19 · `Primary.TButton` 15 · `Compact.TButton` 10 ·
`Section.TLabelframe` 9 · `StepDone.TButton` 3 · `StepBusy.TButton` 3 · `App.TFrame` 3 ·
`Status.TLabel` 2 · `Safe.TButton` 2 · `Header.TLabel` 2 · `HeaderSub.TLabel` 2

→ **위젯 코드는 한 줄도 안 고친다.**

---

## 2단계 — 리터럴 색 90곳

### 2-1. 단순 치환 (판단 불필요, 79곳)

| 현재 | 역할 | → 토큰 | 곳 |
|---|---|---|---|
| `background="white"` | 배경 | `C.SURFACE` | 47 |
| `foreground="#526574"` | 글자 | `C.TEXT_MUTED` | 14 |
| `foreground="#6b7780"` | 글자 | `C.TEXT_MUTED` | 10 |
| `fg="#526574"` | 글자 | `C.TEXT_MUTED` | 2 |
| `bg="#eef3f6"` | 배경 | `C.SURFACE_SOFT` | 2 |
| `foreground="#75838d"` | 글자 | `C.TEXT_FAINT` | 2 |
| `fg="#DCE4EC"` | 글자 | `C.TEXT_ON_DARK_SOFT` | 1 |
| `background="#fbfcfd"` | 배경 | `C.SURFACE` | 1 |

**줄 번호**

- `background="white"` → `C.SURFACE` —
  L571, 675, 691, 701, 710, 745, 759, 787, 796, 817, 874, 993, 1000, 1043, 1090, 1135, 1148, 1155, 1165, 1211, 1236, 1250, 1283, 1467, 1479, 1492, 1505, 1518, 1581, 1585, 1769, 1856, 1868, 1883, 1893, 1912, 1959, 1992, 2073, 2144, 2153, 2171, 2219, 2239, 2283, 2290, 2502
- `foreground="#526574"` → `C.TEXT_MUTED` —
  L788, 875, 1001, 1044, 1091, 1149, 1237, 1857, 1884, 1894, 1960, 2074, 2145, 2172
- `foreground="#6b7780"` → `C.TEXT_MUTED` —
  L572, 676, 746, 760, 1251, 1283, 1468, 1493, 1519, 1585
- `fg="#526574"` → `C.TEXT_MUTED` — L865, 1013
- `bg="#eef3f6"` → `C.SURFACE_SOFT` — L864, 1012  *(빈 상태 안내 박스)*
- `foreground="#75838d"` → `C.TEXT_FAINT` — L1212, 3575
- `fg="#DCE4EC"` → `C.TEXT_ON_DARK_SOFT` — L509  *(헤더 부제)*
- `background="#fbfcfd"` → `C.SURFACE` — L2337  *(tk.Text 입력창)*

### 2-2. 판단이 들어간 곳 (11곳) — 확인 부탁

| 줄 | 현재 | 제안 토큰 | 근거 |
|---|---|---|---|
| L1136 | `foreground="#7a5b20"` | `C.WARNING_TEXT` | 흰 카드 위 **경고성** 안내(토큰·키체인 만료). 주의 톤 유지 |
| L1913 | `foreground="#9a6a2f"` | `C.WARNING_TEXT` | 흰 카드 위 **경고성** 안내(발행 즉시 공개). 같은 역할 |
| L2220 | `foreground="#4b6070"` | `C.TEXT_MUTED` | 경고 아닌 **일반 안내**(무료 호출량). 경고색 쓰면 과함 |
| L2338 | `foreground="#26343c"` | `C.TEXT_STRONG` | tk.Text 본문 글자 |
| L2339 | `insertbackground="#26343c"` | `C.TEXT_STRONG` | 위 Text 의 **커서색**. 글자색과 같이 가야 함 |
| L1618 | `foreground="white"` | **`C.TEXT_ON_DARK`** | ⚠️ 아래 함정 ① 참조 — `C.SURFACE` 아님 |
| L167 | `BG = "#f3f6f8"` | `C.CANVAS` | 클래스 상수. 3단계 참조 |
| L168 | `NAVY = "#16344c"` | (상수 삭제) | 3단계 참조 |
| L169 | `BLUE = "#2b6f8a"` | (상수 삭제) | 3단계 참조 |
| L170 | `GOLD = "#d6a546"` | **삭제** | ⚠️ **죽은 상수 — 앱 어디에서도 안 쓰인다(0곳)** |
| L171 | `GREEN = "#2f7d61"` | (상수 삭제) | 3단계 참조 |

---

## 3단계 — 심볼 치환 22곳 + 함정 2개

`self.NAVY` / `self.BLUE` / `self.GREEN` / `self.BG` 는 `_configure_style()` 밖에서도 쓰인다.
**같은 상수인데 배경으로도 쓰이고 글자로도 쓰인다 — 여기서 갈라야 한다.**

### `self.NAVY` 19곳 → 역할에 따라 둘로

| 역할 | 줄 | → 토큰 |
|---|---|---|
| **배경** (`bg=` / `background=`) | L501, 508, 1617 | `C.INK` |
| **글자** (`foreground=`) | L692, 702, 711, 994, 1156, 1166, 1480, 1506, 1581, 1869, 1993, 2154, 2240, 2284, 2291, 2503 | **`C.TEXT_STRONG`** |

### 나머지

| 줄 | 현재 | → 토큰 | 맥락 |
|---|---|---|---|
| L188 | `bg=self.BG` | `C.CANVAS` | 루트 창 배경 |
| L3571 | `foreground=self.GREEN` | `C.SUCCESS_TEXT` | 진행 단계 — 완료 |
| L3573 | `foreground=self.BLUE` | `C.CORAL_DEEP` | 진행 단계 — 현재 |
| L3575 | `foreground="#75838d"` | `C.TEXT_FAINT` | 진행 단계 — 대기 (2-1 에도 포함) |

### ⚠️ 함정 ① — `white` 를 전부 `C.SURFACE` 로 바꾸면 안 된다

`claude_theme.py` 의 `MIGRATION` 표는 `"white": "C.SURFACE"` 로 뭉뚱그려 놨다.
48곳 중 47곳은 맞지만 **L1618 한 곳은 툴팁의 글자색**이다(배경이 `self.NAVY`).

| 매핑 | 다크모드 명암비 | 결과 |
|---|---|---|
| `C.SURFACE` (표 그대로) | **1.16:1** | ❌ 글자가 배경에 묻혀 사라짐 |
| `C.TEXT_ON_DARK` (제안) | **18.31:1** | 통과 |

### ⚠️ 함정 ② — `self.NAVY` 를 전부 `C.INK` 로 바꾸면 안 된다

라이트모드에선 `C.INK` 와 `C.TEXT_STRONG` 이 **둘 다 `#141413` 로 값이 같다.** 그래서 지금 화면으로는 차이가 안 보인다.
다크모드에서 갈라진다 — `C.INK`는 어두운 **면** 색(`#0F0E0D`), `C.TEXT_STRONG`은 밝은 **글자** 색(`#FAF9F5`).

| 글자 16곳의 매핑 | 다크모드 명암비 | 결과 |
|---|---|---|
| `C.INK` | **1.16:1** | ❌ 검은 글자 + 검은 카드 |
| `C.TEXT_STRONG` (제안) | **15.82:1** | 통과 |

> 다크모드를 안 쓸 계획이어도 이대로 가는 편이 낫다. 지금 화면은 어차피 동일하고, 나중에 다크를 켤 때 16곳을 다시 찾을 필요가 없다.

---

## 4단계 — 검증

제안한 매핑 전부를 실측했다(WCAG AA 본문 기준 4.5:1).

| 매핑 | 실측 | 판정 |
|---|---|---|
| `TEXT_STRONG` / 흰 카드 | 18.43:1 | 통과 |
| `TEXT_ON_DARK` / `INK` | 17.50:1 | 통과 |
| `SUCCESS_TEXT` / 흰 카드 | 6.83:1 | 통과 |
| `TEXT_ON_DARK_SOFT` / `INK` | 6.81:1 | 통과 |
| `WARNING_TEXT` / 흰 카드 | 6.51:1 | 통과 |
| `TEXT_MUTED` / 흰 카드 | 5.41:1 | 통과 |
| `TEXT_FAINT` / 흰 카드 | 4.80:1 | 통과 |
| `TEXT_MUTED` / `SURFACE_SOFT` | 4.77:1 | 통과 |
| `CORAL_DEEP` / 흰 카드 | 4.51:1 | 통과 |

**미달 0건.**

---

## 남는 것 (치환으로 안 되는 것)

- **`tk.*` 위젯 41곳** — `tk.Frame` 등은 ttk 스타일을 안 따른다. 위 표의 `bg=`/`fg=` 치환으로 색은 맞지만, **모서리 둥글기·그림자는 안 된다.** 카드 느낌이 필요한 곳은 `claude_theme` 의 `Card` / `RoundedButton` 헬퍼로 감싸야 한다 — 이건 치환이 아니라 코드 수정이라 3단계와 별개.
- **`relief="solid", borderwidth=1`** (L867~868, L1016~1017, L2331~2332) — tk 위젯의 이 테두리는 색 지정이 안 돼 OS 기본색으로 나온다. 헤어라인(`C.LINE`)으로 바꾸려면 `highlightthickness` + `highlightbackground` 방식으로 교체 필요.
- **`demo_theme.py` L14 `geometry("880x880")`** — 내용보다 작아 하단 액션 바가 잘린다. `880x1000` 이면 정상(확인함). 데모 파일만의 문제.

---

## 미확정 (사용자 확인 대기)

1. 2-2 표의 11곳 제안 토큰 — 특히 L2220 을 `TEXT_MUTED`(일반)로 볼지 `WARNING_TEXT`(경고)로 볼지.
2. `GOLD` 상수 삭제 여부 — 지금은 0곳에서 쓰이지만 의도적으로 남겨둔 것인지.
3. 다크모드 지원 계획 유무 — 없다면 함정 ①② 는 지금 화면상 차이가 없다.
